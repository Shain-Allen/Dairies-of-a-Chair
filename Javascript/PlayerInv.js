/*window.PlayerInv = function (wood, plastic, spareWheel, paint) {
    this.wood = wood || 0;
    this.plastic = plastic || 0;
    this.spareWheel = spareWheel || 0;
    this.paint = paint || 0;
};

PlayerInv.prototype._init = function (obj) {
    Object.keys(obj).forEach(function (pn) {
        this[pn] = clone(obj[pn]);
    }, this);
}*/